{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the single Hearsay window. Deliberately narrow: no HTTP client, no shell access, no filesystem write permissions handed to the frontend — the save sheet returns a path the Rust side writes to, it does not hand the webview the disk.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:default",
    "dialog:allow-ask",
    "dialog:allow-message",
    "dialog:allow-save",
    "global-shortcut:default",
    "opener:allow-open-url",
    "notification:default"
  ]
}
