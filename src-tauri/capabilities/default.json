{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the single Hearsay window. Deliberately narrow: no HTTP client, no shell access, no filesystem write permissions handed to the frontend.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:default",
    "dialog:allow-ask",
    "dialog:allow-message",
    "global-shortcut:default",
    "opener:allow-open-url",
    "notification:default"
  ]
}
